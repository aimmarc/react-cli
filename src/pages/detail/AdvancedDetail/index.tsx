import { PageWrapper } from "@/components";
import React from "react";

const AdvancedDetail: React.FC = (): React.ReactElement => {
    return (
        <PageWrapper bg title="高级详情页">
            000
        </PageWrapper>
    );
};

export default AdvancedDetail;
