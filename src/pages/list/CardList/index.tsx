import { PageWrapper } from "@/components";
import React from "react";

const CardList: React.FC = (): React.ReactElement => {
    return (
        <PageWrapper title="卡片列表" bg>
            000
        </PageWrapper>
    );
};

export default CardList;
