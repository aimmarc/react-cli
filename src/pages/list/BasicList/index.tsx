import { PageWrapper } from "@/components";
import React from "react";

const BasicList: React.FC = (): React.ReactElement => {
    return (
        <PageWrapper title="标准列表" bg>
            000
        </PageWrapper>
    );
};

export default BasicList;
