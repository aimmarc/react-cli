import React from "react";
import { PageWrapper } from "@/components";

const Analysis: React.FC = (): React.ReactElement => {
    return <PageWrapper bg fit title="分析页" goBack></PageWrapper>;
};

export default Analysis;
