import React from "react";
import { PageWrapper } from "@/components";

const Setting: React.FC = (): React.ReactElement => {
    return <PageWrapper bg fit title="设置" goBack></PageWrapper>;
};

export default Setting;
